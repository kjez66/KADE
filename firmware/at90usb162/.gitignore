# The repo root ignores *.hex and *.eep as AVR build output. These images are
# not build output -- they are extracted from / generated against the loader
# database and committed on purpose.
!*.hex
!*.eep
